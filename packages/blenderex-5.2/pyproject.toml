[project]
name = "blenderex-5.2"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["blenderex"]

[project.scripts]
"blender5.2" = "blenderex:blender_5_2"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"

[tool.uv.sources]
blenderex = { workspace = true }
